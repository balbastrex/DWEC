// document.title = 'New title';

// const heading = document.querySelector('h1');
// heading.style.fontsice = '36px';
// heading.style.fontFamily = 'sans-serif';

// const links = document.getElementsByTagName('a');

// [...links].forEach(links => {
// links.style.textDecoration = 'none';
// links.style.color = 'orange';
// links.style.padding = '1em';
// links.style.backgroundColor = 'blue';
// links.style.width = '10em';
// links.style.textAlign = 'center';
// });


// const buttonsContainer = document.getElementsByClassName('buttons-container');

// Array.from(buttonsContainer).forEach(container => container.style.backgroundColor = 'salmon'); 

// const buttons = document.querySelectorAll('button[id]');

// buttons.forEach(button => button.addEventListener('click', () => window.alert(button.id)));

// //buttons.forEach((button, index) => button.addEventListener(('click') , () => window.alert(`Yo soy el botón ${index}`)));

// buttons.forEach((button, index) => button.addEventListener(('click') , () => window.alert('Yo soy el botón ' + index)));

// const myParagraph = document.querySelector('p');

// myParagraph.textContent = 'Mi nuevo contenido';

// myParagraph.addEventListener('click', () => {
//     window.alert('A mí también se me puede registrar un evento click, como puedes ver');
// });

// const linkToGoogle = links[0];

// const linkToAngular = links[1];

// linkToGoogle.addEventListener('click', (event) => {
//     window.alert('A ver si esto funciona');
// });

// linkToGoogle.addEventListener(('click'), (event) => {
//     event.preventDefault();
//     linkToAngular.style.display = 'none'});